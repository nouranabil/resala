<% if @city_branches %>
  $("#select_field_branch_id").html("<%= escape_javascript(select_tag(:branch_id, options_for_select([["أختر الفرع", ""]]) + options_from_collection_for_select(@city_branches, "id", "name", 0), :name=>@select_tag_name ? @select_tag_name : "branch_id", :class=>"text") + (" <span class='mandatory'> *</span> ").html_safe) %>");
<% end %>
